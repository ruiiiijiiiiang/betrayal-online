{
  "name": "@betrayal/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for betrayal-online (client + server)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist && rimraf node_modules && rimraf tsconfig.tsbuildinfo"
  },
  "devDependencies": {}
}